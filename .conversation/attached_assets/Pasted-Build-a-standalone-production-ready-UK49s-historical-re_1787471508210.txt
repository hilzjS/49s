Build a standalone production-ready UK49s historical results scraper ONLY (no prediction engine) that scrapes these exact sources: LUNCHTIME `https://uk.lottonumbers.com/uk49s-lunchtime/results/{YEAR}` and TEATIME `https://uk.lottonumbers.com/uk49s-teatime/results/{YEAR}`; support URLs such as `https://uk.lottonumbers.com/uk49s-lunchtime/results/2015` and `https://uk.lottonumbers.com/uk49s-teatime/results/2015`; accept `drawType` (`lunchtime` or `teatime`) and `year` parameters; scrape every available draw on the selected yearly page, including all monthly sections, and do not stop after the first results block; parse the draw date, exactly 6 main winning numbers and the 7th Booster Ball, validate all numbers are integers from 1–49, preserve the original date and normalize it to ISO `YYYY-MM-DD`, assign `draw_type` and `year`, remove duplicates using `draw_type + draw_date`, sort chronologically, reject malformed records, and return clean JSON using `{success,drawType,year,source,count,results}` where each result is `{draw_date,draw_type,winning_numbers:[n1,n2,n3,n4,n5,n6],booster_ball:n7}`; create API endpoints `/api/scrape/lunchtime/:year`, `/api/scrape/teatime/:year`, `/api/scrape/:drawType/:year`, `/api/scrape/all/:year`, `/api/scrape/all`, and `/api/health`; `/api/scrape/all/:year` must scrape both Lunch and Tea for that year, while `/api/scrape/all` must iterate from 1997 through the current year and collect both draw types; implement reliable HTTP requests with browser-like headers, timeout, retries, rate limiting, error handling and logging; implement robust HTML parsing with fallback selectors so minor website HTML changes do not immediately break the scraper; log every requested URL, HTTP status, records discovered, accepted records, rejected records, duplicate records and parsing errors; add caching and a `forceRefresh` option; add a simple web UI with Lunch/Tea/Both selection, year/year-range input, scrape button, progress display, record count, failed URLs, validation errors, result preview and JSON/CSV download; add tests specifically for the 2015 Lunchtime and 2015 Teatime pages and verify that the scraper extracts the complete historical dataset, correctly separates 6 main numbers from the Booster Ball, removes duplicates and returns valid JSON; provide a README with exact API usage examples so another AI application can consume the scraper; keep the project completely independent from any prediction, AI, optimization or backtesting logic—the final product must be a reliable UK49s data API that another AI can call to obtain historical Lunchtime and Teatime results.
